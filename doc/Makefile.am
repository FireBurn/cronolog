## Process this file with automake to create Makefile.in

man_MANS = cronolog.1m cronosplit.1m
info_TEXINFOS = cronolog.texi
EXTRA_DIST = $(man_MANS)

if BUILD_INFO
INFO_DEPS = cronolog.info
else
INFO_DEPS =
endif
